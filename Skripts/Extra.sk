on craft:
	cancel the event

on block damaging:
	if target block is not bedrock or barrier:
		break target block

on inventory open:
	if inventory name of player's current inventory is not "&cBlocks":
		close player's current inventory
		make player execute command "/blocks"		